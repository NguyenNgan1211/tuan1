#include <iostream>
using namespace std;
 int main(){
 	int t  =3, y=3,z=3 ;
 	cout<<"a, x = "<<(float)(-3+4%5/2)<<endl; // % / +
 	cout<<"b, y = "<<(!y % 4)<<endl;// !y = 0
 	cout<<"c, z = "<<(!(++z % 4))<<endl;//4 chia 4 la 0 , ! = 1
 	cout<<"c, t = "<<(!(++t % 4));
}
